/**
 *
 * En este paquete de excepciones se encuentran todas las excepciones de nuestra aplicacion,
 * las cuales mostramos por pantalla para que el usuario que esta interaccionando con la aplicacion,
 * que sea consciente porque esta esta fallando.
 */
package exceptions;
